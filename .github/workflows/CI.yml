name: CI

on: push

jobs:
  install-cache:
    name: Install cache
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Commit
        uses: actions/checkout@v5

      - name: Install Node.js
        uses: actions/setup-node@master
        with:
          node-version: 24
          cache: 'yarn'

      - name: Install project dependencies
        run: yarn --frozen-lockfile

  eslint-check:
    name: Eslint check
    runs-on: ubuntu-latest
    needs: install-cache
    steps:
      - uses: actions/checkout@v5

      - name: Install Node.js
        uses: actions/setup-node@master
        with:
          node-version: 24
          cache: 'yarn'

      - name: Install project dependencies
        run: yarn --frozen-lockfile

      - name: Run ESLint
        run: npm run lint

      - name: Send Telegram Failure Message
        if: failure()
        uses: PacificPromise/macos-telegram-action@main
        with:
          type: topic
          message: |
            ❗️ Произошла ошибка ESLint

            🔗 Ссылка на пайплайн 🔗
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: eslint-check
    steps:
      - uses: actions/checkout@v5

      - name: Install Node.js
        uses: actions/setup-node@master
        with:
          node-version: 24
          cache: 'yarn'

      - name: Install project dependencies
        run: yarn --frozen-lockfile

      - name: Run build
        run: npm run build

      - name: Send Telegram Failure Message
        if: failure()
        uses: PacificPromise/macos-telegram-action@main
        with:
          type: topic
          message: |
            ❗️ Произошла ошибка во время сборки

            🔗 Ссылка на пайплайн 🔗
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
